my_tuple = (1, 2, 3)

print(my_tuple)
# my_tuple[0] = (
#     12  # Cannot change the Tuple gives: 'tuple' object does not support item assignment
# )


# Mixed Data type are allowed
info = ("Pramod", 34, True)

# Tuple with one element
single(
    3,
)
print(type(single))


#### Tuples are immutable, but we can create a new tuple from existing tuples
tuple1 = (1, 2, 3)
